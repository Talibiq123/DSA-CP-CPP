#include<bits/stdc++.h>
using namespace std;

// Write a program in C++ to check Whether you are adult or not

int main() {
    int age;
    cout << "Enter Your Age : ";
    cin >> age;

    if (age >= 18) {
        cout << "You are not adult.";
    } else {
        cout << "You are not adult.";
    }
    return 0;
}
